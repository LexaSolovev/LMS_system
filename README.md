# LMS System (Learning Management System)

Система управления обучением с REST API, построенная на Django и Django REST Framework.

## 📋 Содержание

- [Функциональность](#-функциональность)
- [Технологии](#-Технологии)
- [Установка и запуск](#-установка-и-запуск)
- [Быстрый запуск с docker-compose](#быстрый-запуск-с-docker-compose)
- [Локальная разработка (без docker-compose)](#локальная-разработка-без-docker-compose)
- [API Эндпоинты](#-api-эндпоинты)
- [Аутентификация](#-аутентификация)
- [Права доступа](#-права-доступа)
- [Фильтрация и поиск](#-фильтрация-и-поиск)
- [Примеры запросов](#-примеры-запросов)
- [Модели данных](#-модели-данных)

## 🚀 Функциональность

- **Управление курсами и уроками** - полный CRUD для образовательного контента
- **Система пользователей** - регистрация, аутентификация, профили
- **Платежная система** - отслеживание оплат курсов и уроков
- **Разграничение прав доступа** - роли пользователей, модераторов и администраторов
- **JWT аутентификация** - безопасный доступ к API
- **Фильтрация и поиск** - удобный поиск и сортировка данных
- **REST API** - полное API для интеграции с фронтендом

## 🛠 Технологии

- **Backend**: Django 4.2+, Django REST Framework
- **Аутентификация**: JWT (Simple JWT)
- **База данных**: SQLite (разработка) / PostgreSQL (продакшен)
- **Фильтрация**: Django Filter
- **Управление зависимостями**: Poetry
- **Документация**: Swagger/OpenAPI (можно добавить)

# ⚙️ Установка и запуск

## Быстрый запуск с docker-compose:

1. Клонирование репозитория
```bash

git clone https://github.com/LexaSolovev/LMS_system.git
cd LMS_system
```

2. Настройка переменных окружения

Создайте файл конфигурации:
```bash

mkdir -p config
cp config/.env.sample config/.env
```
Отредактируйте файл config/.env при необходимости:
```bash

# Открыть файл для редактирования
nano config/.env

# Или в Windows
notepad config\.env
```

### 3.Запуск приложения
```bash
# Собрать и запустить все сервисы
docker-compose up -d

# Или с пересборкой образов
docker-compose up -d --build
```
### 4. Проверка работы сервисов
```bash
# Просмотр логов
docker-compose logs -f

# Просмотр статуса контейнеров
docker-compose ps
```
## Локальная разработка (без docker-compose)
### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd LMS_system
```

### 2. Установка Poetry (если не установлен)

```bash
# Windows
curl -sSL https://install.python-poetry.org | python3 -

# Linux/Mac
curl -sSL https://install.python-poetry.org | python3 -

# Проверка установки
poetry --version
```

### 3. Установка зависимостей через Poetry

```bash
# Установка зависимостей
poetry install

# Активация виртуального окружения
poetry shell
```

### 4. Настройка базы данных

```bash
# Применение миграций
python manage.py migrate

# Создание суперпользователя (опционально)
python manage.py createsuperuser
```

### 5. Создание тестовых данных

```bash
# Заполнение базы тестовыми данными
python manage.py fill_test_data --clear
```

### 6. Запуск сервера разработки

```bash
# Запуск сервера
python manage.py runserver

# Или с указанием порта
python manage.py runserver 8000
```

Приложение будет доступно по адресу: `http://localhost:8000`

### 7. Дополнительные команды Poetry

```bash
# Добавление новых зависимостей
poetry add package_name

# Добавление зависимостей для разработки
poetry add --dev package_name

# Обновление зависимостей
poetry update

# Просмотр установленных пакетов
poetry show

# Создание requirements.txt (если нужно)
poetry export -f requirements.txt --output requirements.txt
```

## 🔐 Аутентификация

Система использует JWT (JSON Web Tokens) для аутентификации.

### Получение токена доступа:

```bash
POST /api/users/token/
Content-Type: application/json

{
    "email": "user@example.com",
    "password": "password123"
}
```

Ответ:
```json
{
    "refresh": "xxx.yyy.zzz",
    "access": "aaa.bbb.ccc"
}
```

### Обновление токена:

```bash
POST /api/users/token/refresh/
Content-Type: application/json

{
    "refresh": "xxx.yyy.zzz"
}
```

### Использование токена в запросах:

```http
Authorization: Bearer aaa.bbb.ccc
```

## 👥 Права доступа

### Роли пользователей:

1. **Обычный пользователь**
   - Может создавать, просматривать, редактировать и удалять только свои курсы и уроки
   - Видит только свои платежи

2. **Модератор** (группа 'moderators')
   - Может просматривать и редактировать ВСЕ курсы и уроки
   - Не может создавать или удалять курсы и уроки
   - Видит все платежи (только просмотр)

3. **Администратор**
   - Полный доступ ко всем функциям системы

## 📊 API Эндпоинты

### 🔐 Аутентификация

| Метод | URL | Описание | Доступ |
|-------|-----|-----------|---------|
| POST | `/api/users/token/` | Получение JWT токена | Все |
| POST | `/api/users/token/refresh/` | Обновление JWT токена | Все |
| POST | `/api/users/register/` | Регистрация нового пользователя | Все |

### 📚 Материалы (Courses & Lessons)

#### Курсы

| Метод | URL | Описание | Доступ |
|-------|-----|-----------|---------|
| GET | `/api/materials/courses/` | Список всех курсов | Аутентифицированные |
| POST | `/api/materials/courses/` | Создание нового курса | Владельцы (не модераторы) |
| GET | `/api/materials/courses/{id}/` | Детали курса | Владелец или модератор |
| PUT | `/api/materials/courses/{id}/` | Полное обновление курса | Владелец или модератор |
| PATCH | `/api/materials/courses/{id}/` | Частичное обновление курса | Владелец или модератор |
| DELETE | `/api/materials/courses/{id}/` | Удаление курса | Только владелец |

#### Уроки

| Метод | URL | Описание | Доступ |
|-------|-----|-----------|---------|
| GET | `/api/materials/lessons/` | Список всех уроков | Аутентифицированные |
| POST | `/api/materials/lessons/` | Создание нового урока | Владельцы (не модераторы) |
| GET | `/api/materials/lessons/{id}/` | Детали урока | Владелец или модератор |
| PUT | `/api/materials/lessons/{id}/` | Полное обновление урока | Владелец или модератор |
| PATCH | `/api/materials/lessons/{id}/` | Частичное обновление урока | Владелец или модератор |
| DELETE | `/api/materials/lessons/{id}/` | Удаление урока | Только владелец |

### 👤 Пользователи

| Метод | URL | Описание | Доступ |
|-------|-----|-----------|---------|
| GET | `/api/users/users/` | Список пользователей | Администраторы |
| POST | `/api/users/users/` | Создание пользователя | Все (регистрация) |
| GET | `/api/users/users/{id}/` | Детали пользователя | Владелец или администратор |
| PUT | `/api/users/users/{id}/` | Обновление пользователя | Владелец или администратор |
| PATCH | `/api/users/users/{id}/` | Частичное обновление | Владелец или администратор |
| DELETE | `/api/users/users/{id}/` | Удаление пользователя | Владелец или администратор |
| GET | `/api/users/users/profile/` | Профиль текущего пользователя | Владелец |
| PUT | `/api/users/users/update_profile/` | Обновление профиля | Владелец |

### 💰 Платежи

| Метод | URL | Описание | Доступ |
|-------|-----|-----------|---------|
| GET | `/api/users/payments/` | Список платежей | Владелец или модератор |
| POST | `/api/users/payments/` | Создание платежа | Аутентифицированные |
| GET | `/api/users/payments/{id}/` | Детали платежа | Владелец или модератор |
| PUT | `/api/users/payments/{id}/` | Обновление платежа | Только владелец |
| PATCH | `/api/users/payments/{id}/` | Частичное обновление | Только владелец |
| DELETE | `/api/users/payments/{id}/` | Удаление платежа | Только владелец |

## 🔍 Фильтрация и поиск

### Платежи

Доступны следующие параметры фильтрации для платежей:

- **По курсу**: `GET /api/users/payments/?paid_course=1`
- **По уроку**: `GET /api/users/payments/?paid_lesson=1`
- **По способу оплаты**: `GET /api/users/payments/?payment_method=cash`
- **Сортировка по дате**:
  - По возрастанию: `GET /api/users/payments/?ordering=payment_date`
  - По убыванию: `GET /api/users/payments/?ordering=-payment_date`
- **По сумме**:
  - Минимальная сумма: `GET /api/users/payments/?amount__gte=1000`
  - Максимальная сумма: `GET /api/users/payments/?amount__lte=5000`
- **По диапазону дат**:
  - С: `GET /api/users/payments/?payment_date__gte=2024-01-01`
  - По: `GET /api/users/payments/?payment_date__lte=2024-01-31`

### Курсы и уроки

- **Поиск по названию**: `GET /api/materials/courses/?search=python`
- **Поиск по описанию**: `GET /api/materials/courses/?search=программирование`
- **Сортировка**: `GET /api/materials/courses/?ordering=name`

## 💡 Примеры запросов

### Создание курса

```bash
POST /api/materials/courses/
Authorization: Bearer ваш_jwt_токен
Content-Type: application/json

{
    "name": "Новый курс программирования",
    "description": "Описание нового курса",
    "preview": null
}
```

### Создание урока

```bash
POST /api/materials/lessons/
Authorization: Bearer ваш_jwt_токен
Content-Type: application/json

{
    "name": "Введение в тему",
    "description": "Описание урока",
    "video_link": "https://youtube.com/watch?v=example",
    "course": 1
}
```

### Регистрация пользователя

```bash
POST /api/users/register/
Content-Type: application/json

{
    "email": "newuser@example.com",
    "password": "securepassword123",
    "password2": "securepassword123",
    "phone": "+79161234567",
    "city": "Москва"
}
```

## 🗃 Модели данных

### Course (Курс)
- name (Название)
- preview (Превью)
- description (Описание)
- owner (Владелец)
- created_at (Дата создания)
- updated_at (Дата обновления)

### Lesson (Урок)
- name (Название)
- description (Описание)
- preview (Превью)
- video_link (Ссылка на видео)
- course (Курс)
- owner (Владелец)
- created_at (Дата создания)
- updated_at (Дата обновления)

### Payment (Платеж)
- user (Пользователь)
- payment_date (Дата оплаты)
- paid_course (Оплаченный курс)
- paid_lesson (Оплаченный урок)
- amount (Сумма оплаты)
- payment_method (Способ оплаты: cash/transfer)

### User (Пользователь)
- email (Email)
- phone (Телефон)
- city (Город)
- avatar (Аватар)
- groups (Группы)
